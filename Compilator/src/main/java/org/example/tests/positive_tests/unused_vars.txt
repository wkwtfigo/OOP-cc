class A is
  var a : Integer(2)

  this() is
    print a
  end
end

class Main is
    method start() is 
      var a : A()
      var b : Boolean(false).Or(false)
      var i : Integer(1).Plus(b)
    end
end
