class Main is
    var a : Integer(2)

    method getA() : Integer => a

    method incA(b : Array[Integer]) : Integer is 
        var max : Integer(1)
        b := a
        return max
        var i : a
    end

    this() is
        a := Integer(2)
    end
end